# Air configuration for hot-reloading development
# https://github.com/air-verse/air

root = "."
tmp_dir = "tmp"

[build]
# Build command - injects version info similar to Taskfile
# Uses shell command substitution to get git info dynamically
cmd = """go build -ldflags "-s -w \
  -X github.com/jmylchreest/tvarr/internal/version.Version=dev \
  -X github.com/jmylchreest/tvarr/internal/version.Commit=$(git rev-parse HEAD 2>/dev/null || echo unknown) \
  -X github.com/jmylchreest/tvarr/internal/version.Date=$(date -u +%Y-%m-%dT%H:%M:%SZ) \
  -X github.com/jmylchreest/tvarr/internal/version.Branch=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo unknown) \
  -X github.com/jmylchreest/tvarr/internal/version.TreeState=$(if git diff --quiet 2>/dev/null; then echo clean; else echo dirty; fi)" \
  -o ./tmp/tvarr ./cmd/tvarr"""
# Binary entrypoint with arguments (replaces deprecated bin/full_bin)
# Default to debug logging for development visibility
entrypoint = ["./tmp/tvarr", "serve", "--log-level", "debug"]
# Watch these file extensions
include_ext = ["go", "tpl", "tmpl", "html", "yaml", "yml"]
# Watch these directories
include_dir = ["cmd", "internal", "pkg"]
# Exclude directories
exclude_dir = ["assets", "tmp", "vendor", "frontend", "node_modules", "dist", "testdata", ".git"]
# Exclude files matching these patterns
exclude_file = []
# Exclude unchanged files
exclude_unchanged = true
# Follow symlinks
follow_symlink = true

[log]
# Log time in output
time = true
# Main log (shows what air is doing)
main_only = false

[color]
main = "magenta"
watcher = "cyan"
build = "yellow"
runner = "green"

[misc]
# Delete tmp directory on exit
clean_on_exit = true
