{{- if eq (include "tvarr.transcoderEnabled" .) "true" }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tvarr.fullname" . }}-transcoder
  labels:
    {{- include "tvarr.labels" . | nindent 4 }}
    app.kubernetes.io/component: transcoder
spec:
  replicas: {{ .Values.transcoder.replicaCount }}
  selector:
    matchLabels:
      {{- include "tvarr.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: transcoder
  {{- with .Values.transcoder.strategy }}
  strategy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  template:
    metadata:
      {{- with .Values.transcoder.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "tvarr.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: transcoder
    spec:
      enableServiceLinks: {{ .Values.enableServiceLinks | default false }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "tvarr.serviceAccountName" . }}
      {{- if and .Values.transcoder.gpu.enabled (eq .Values.transcoder.gpu.type "nvidia") }}
      runtimeClassName: {{ .Values.transcoder.gpu.nvidia.runtimeClassName }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.transcoder.podSecurityContext | nindent 8 }}
      containers:
        - name: transcoder
          securityContext:
            {{- toYaml .Values.transcoder.securityContext | nindent 12 }}
          image: "{{ .Values.transcoder.image.repository }}:{{ .Values.transcoder.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.transcoder.image.pullPolicy }}
          env:
            # Required: Coordinator gRPC URL
            - name: TVARR_COORDINATOR_URL
              value: "{{ include "tvarr.fullname" . }}:{{ .Values.service.grpcPort | default 9090 }}"
            # User/Group for permissions
            - name: PUID
              value: {{ .Values.env.PUID | default "1000" | quote }}
            - name: PGID
              value: {{ .Values.env.PGID | default "1000" | quote }}
            - name: TZ
              value: {{ .Values.env.TZ | default "UTC" | quote }}
            # Logging level
            - name: TVARR_LOGGING_LEVEL
              value: {{ .Values.env.TVARR_LOGGING_LEVEL | default "info" | quote }}
            # Transcoder-specific env vars
            {{- range $key, $value := .Values.transcoder.env }}
            {{- if $value }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            {{- end }}
            # Use pod name as daemon name if not explicitly set
            {{- if not .Values.transcoder.env.TVARR_DAEMON_NAME }}
            - name: TVARR_DAEMON_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            {{- end }}
            {{- with .Values.transcoder.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          resources:
            {{- if or .Values.transcoder.resources (and .Values.transcoder.gpu.enabled (or (eq .Values.transcoder.gpu.type "nvidia") (eq .Values.transcoder.gpu.type "intel"))) }}
            {{- $resources := .Values.transcoder.resources | default dict }}
            {{- $limits := $resources.limits | default dict }}
            {{- if and .Values.transcoder.gpu.enabled (eq .Values.transcoder.gpu.type "nvidia") }}
            {{- $limits = merge $limits (dict "nvidia.com/gpu" 1) }}
            {{- else if and .Values.transcoder.gpu.enabled (eq .Values.transcoder.gpu.type "intel") }}
            {{- $limits = merge $limits (dict "gpu.intel.com/i915" 1) }}
            {{- end }}
            {{- if $limits }}
            limits:
              {{- toYaml $limits | nindent 14 }}
            {{- end }}
            {{- if $resources.requests }}
            requests:
              {{- toYaml $resources.requests | nindent 14 }}
            {{- end }}
            {{- else }}
            {}
            {{- end }}
          {{- if and .Values.transcoder.gpu.enabled (or (eq .Values.transcoder.gpu.type "intel") (eq .Values.transcoder.gpu.type "amd")) }}
          volumeMounts:
            - name: dri
              mountPath: /dev/dri
          {{- end }}
          # Transcoder doesn't expose HTTP, just check process is running
          livenessProbe:
            exec:
              command:
                - pgrep
                - -f
                - tvarr-ffmpegd
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            exec:
              command:
                - pgrep
                - -f
                - tvarr-ffmpegd
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
      {{- if and .Values.transcoder.gpu.enabled (or (eq .Values.transcoder.gpu.type "intel") (eq .Values.transcoder.gpu.type "amd")) }}
      volumes:
        - name: dri
          hostPath:
            path: {{ .Values.transcoder.gpu.intel.devicePath | default "/dev/dri" }}
      {{- end }}
      {{- with .Values.transcoder.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.transcoder.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.transcoder.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.transcoder.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
